
var arr = [];
for (var i = 0, length = arr.length; i < length; i += 1)
{
    // << insert code here >>
}

var person = {};
for (var name in person)
{
    if (person.hasOwnProperty(name))
    {
        // << insert code here >>
    }
}